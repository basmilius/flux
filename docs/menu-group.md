# <flux-menu-group/>

A group within a `<flux-menu/>` displays its children vertically, but the gap between
them is smaller.

### Slots

| Name    | Context |
|---------|---------|
| default | {}      |
