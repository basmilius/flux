{
    "name": "flux-workspace",
    "private": true,
    "scripts": {
        "build": "pnpm --filter '@basmilius/*' -r build",
        "dev": "pnpm --filter '@basmilius/*' -r --parallel dev"
    },
    "engines": {
        "node": ">=23"
    },
    "workspaces": [
        "docs",
        "packages/*"
    ]
}
